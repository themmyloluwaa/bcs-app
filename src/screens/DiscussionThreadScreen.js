import React from "react";
import { View, Text } from "react-native";

const DiscussionScreenThread = ({ navigatio }) => {
  return (
    <View>
      <Text>hi</Text>
    </View>
  );
};

export default DiscussionScreenThread;
